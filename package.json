{
  "name": "review-loop",
  "version": "0.3.0",
  "description": "A Claude Code Stop hook that keeps the fix-review-fix loop running until findings are resolved.",
  "license": "MIT",
  "type": "module",
  "files": [
    "hooks/",
    "lib/",
    "setup",
    "bin/",
    "README.md",
    "LICENSE",
    "VERSION",
    "CHANGELOG.md"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "npm run typecheck && vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
